# Disaster Response Tracker API

A FastAPI service for reporting, managing, and voting on disaster incidents.

## Project layout

The backend application, database migrations, dependencies, and tests live in
[`backend/`](backend/). See the backend README for setup and run instructions.
